{% extends 'base.html.twig' %}

{% block title %}Genre{% endblock %}

{% block body %}
    <h1>Genre</h1>

    <table class="uk-table uk-table-divider">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ genre.nom }}</td>
            </tr>
        </tbody>
    </table>

   <center> <a class="uk-button uk-button-secondary" href="{{ path('genre_index') }}">back to list</a>
{% if app.user %}    
    {% if app.user.roles[0] == 'ROLE_ADMIN' %}    
    <a class="uk-button uk-button-primary" href="{{ path('genre_edit', {'id': genre.id}) }}">edit</a></center>
    {% endif %}
{% endif %}
    {{ include('genre/_delete_form.html.twig') }}
{% endblock %}
